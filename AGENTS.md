# AGENTS.md

## Cursor Cloud specific instructions

This repository (`cjs_foto`) is an "Artist's website" project. As of initial setup, it is a greenfield repo containing only a `README.md` — no application code, frameworks, or dependencies exist yet.

- **No build/lint/test/run commands** are available until application code is added.
- **No services** need to be started.
- When application code is introduced, update this section and the VM update script accordingly (e.g., add `npm install`, `pip install -r requirements.txt`, or the appropriate dependency install command).
